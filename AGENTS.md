# Local Project Rules

These rules apply to work performed inside this project at
`/Users/nikita/Desktop/codex`.

## Scope

- The project root `/Users/nikita/Desktop/codex` is the only working directory.
- Do not create, edit, move, or delete files outside this project unless the user explicitly asks for it.
- Prefer commands that operate only on files inside this project.

## Network Use

- Network access is considered allowed by project policy when it is needed to complete the user's task.
- If the current Codex environment or sandbox still requires approval for internet access, that environment rule takes precedence.
- Do not use network access for unrelated background activity.
- When network access is used, keep it limited to what is necessary for the task.

## Safety

- Never run destructive commands outside the project.
- Do not change system-wide settings or Codex app settings from this project unless the user explicitly requests that work and the environment allows it.

## Collaboration

- Favor doing the work directly instead of asking unnecessary clarifying questions.
- Treat these rules as local operating guidance for this repository.
